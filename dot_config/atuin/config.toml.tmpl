inline_height = 10
show_preview = true
exit_mode = "return-query"
history_filter = [
   "^vault",
   "^bw",
   "^export VAULT_ADDR",
   "^export VAULT_TOKEN"
]
enter_accept = true

sync_address = {{ or (.chezmoi.config.data.atuinServer | quote) ("https://api.atuin.sh") }}
